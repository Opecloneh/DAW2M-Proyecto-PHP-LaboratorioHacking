<style>

    .wiki-external-guide {
        margin-top: 40px;
        padding: 20px;
        background: rgba(155, 89, 182, 0.1);
        border-left: 4px solid #9b59b6;
        border-radius: 6px;
    }

    .wiki-external-guide h3 {
        margin-top: 0;
        color: #fff;
        font-size: 1.2rem;
        margin-bottom: 10px;
    }

    .wiki-external-guide p {
        margin: 0;
        color: #e0d3f8;
    }

    .wiki-external-guide a {
        color: rgba(255, 192, 203);
        font-weight: bold;
        text-decoration: none;
        transition: color 0.3s;
    }

    .wiki-external-guide a:hover {
        color: #ffffff;
        text-decoration: underline;
    }

    .wiki-separator {
        margin: 30px 0;
        opacity: 0.2;
    }

    .solution-button {
        background: #9b59b6;
        color: #fff;
        border: none;
        padding: 10px 18px;
        border-radius: 6px;
        cursor: pointer;
        transition: background 0.3s;
    }

    .solution-button:hover {
        background: #6a0dad;
    }

    .solution-box {
        display: none;
        margin-top: 20px;
        background: rgba(46, 204, 113, 0.15);
        padding: 20px;
        border-radius: 8px;
        border: 1px solid #2ecc71;
    }

</style>
<main>
<h1>💻 XSS Reflejado</h1>

<p><strong>Dificultad:</strong> Media</p>

<p>
    En este laboratorio debes explotar un <strong>XSS Reflejado</strong> en un parámetro de búsqueda para mostrar un mensaje de alerta.
</p>

<hr class="wiki-separator">

<h2>🧠 ¿Qué es XSS Reflejado?</h2>

    <p>
        Cross-Site Scripting (XSS) permite inyectar <strong>JavaScript</strong> en el navegador de la víctima.
        En el caso reflejado, el payload viaja en la URL o formulario y se ejecuta inmediatamente al visitar la página.
    </p>

    <p>
        Esto ocurre cuando la aplicación toma datos introducidos por el usuario y los muestra en la página
        sin aplicar ningún tipo de filtrado o escape.
    </p>

    <p>
        Si el servidor devuelve directamente lo que el usuario escribió, el navegador lo interpretará como código
        válido. Si ese contenido incluye etiquetas <code>&lt;script&gt;</code>, el código JavaScript se ejecutará.
    </p>

    <p>
        Esto puede ser usado para mostrar alertas, robar cookies, secuestrar sesiones,
        modificar el contenido de la página o redirigir usuarios maliciosamente.
    </p>

    <hr class="wiki-separator">

    <h2>🎯 Objetivo</h2>

    <p>
        Desplegar una alerta que muestre <code>"1"</code> usando un parámetro vulnerable de la página.
    </p>

    <p>
        Para lograrlo, deberás identificar qué parámetro refleja directamente el valor introducido
        y comprobar si el navegador interpreta tu entrada como código HTML/JavaScript en lugar de texto plano.
    </p>

<hr class="wiki-separator">

<h2>🔐 Solución</h2>

<button onclick="document.getElementById('solucion-xss').style.display='block'; this.style.display='none';"
        class="solution-button">
    Mostrar solución
</button>

<div id="solucion-xss" class="solution-box">

    <p><strong>Payload utilizado:</strong></p>

    <pre>&lt;script&gt;alert("1")&lt;/script&gt;</pre>

    <p>
        Este código se inyecta en el parámetro vulnerable de búsqueda.
        Al cargarse la página, se ejecuta en el navegador.
    </p>

</div>

<hr class="wiki-separator">

<h2>🛡 ¿Cómo se previene?</h2>

<ul>
    <li>Escapar todos los datos que se muestran en HTML</li>
    <li>Usar funciones de sanitización de entrada</li>
    <li>Aplicar Content Security Policy (CSP)</li>
    <li>Evitar inyectar datos del usuario directamente en <code>innerHTML</code></li>
</ul>

<div class="wiki-external-guide">
    <h3>📖 Más información</h3>
    <p>
        Consulta la guía profesional de PortSwigger para profundizar en esta vulnerabilidad:
        <a href="https://portswigger.net/web-security/cross-site-scripting" target="_blank">
            Ver guía completa
        </a>
    </p>
</div>
</main>
