{% extends 'base.html.twig' %}

{% block title %}Tutoriales | VioletBox{% endblock %}

{% block body %}

    <style>
        .wiki-container {
            display: flex;
            min-height: 80vh;
        }

        .wiki-sidebar {
            width: 250px;
            background: rgba(0,0,0,0.2);
            padding: 30px 20px;
            border-right: 1px solid rgba(255,255,255,0.1);
        }

        .wiki-sidebar h3 {
            margin-bottom: 20px;
        }

        .wiki-sidebar a {
            display: block;
            padding: 10px;
            margin-bottom: 8px;
            text-decoration: none;
            color: #fff;
            border-radius: 6px;
            transition: background 0.3s;
        }

        .wiki-sidebar a:hover {
            background: rgba(255,255,255,0.1);
        }

        .wiki-sidebar a.active {
            background: #9b59b6;
        }

        .wiki-content {
            flex: 1;
            padding: 40px;
        }

        .wiki-content pre {
            background: rgba(0,0,0,0.4);
            padding: 15px;
            border-radius: 6px;
            overflow-x: auto;
        }
    </style>

    <div class="wiki-container">

        <aside class="wiki-sidebar">
            <h3>📚 Vulnerabilidades</h3>

            <a href="{{ path('app_tutorial', {vuln: 'introduccion'}) }}"
               class="{% if currentVuln == 'introduccion' %}active{% endif %}">
                Introducción
            </a>

            <a href="{{ path('app_tutorial', {vuln: 'sql-injection'}) }}"
               class="{% if currentVuln == 'sql-injection' %}active{% endif %}">
                SQL Injection
            </a>

            <a href="{{ path('app_tutorial', {vuln: 'xss'}) }}"
               class="{% if currentVuln == 'xss' %}active{% endif %}">
                XSS
            </a>

            <a href="{{ path('app_tutorial', {vuln: 'idor'}) }}"
               class="{% if currentVuln == 'idor' %}active{% endif %}">
                IDOR
            </a>

            <a href="{{ path('app_tutorial', {vuln: 'file-upload'}) }}"
               class="{% if currentVuln == 'file-upload' %}active{% endif %}">
                File Upload
            </a>

        </aside>


        <section class="wiki-content">
            {% include 'tutorial/pages/' ~ currentVuln ~ '.html.twig' %}
        </section>

    </div>

{% endblock %}
